version: '3.8'

services:
  playlist-app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - playlist-data:/app/data
    restart: unless-stopped
    mem_limit: 1024m
    mem_reservation: 256m
    cpus: 0.5

volumes:
  playlist-data:
